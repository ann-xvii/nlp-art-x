require 'rails_helper'

RSpec.describe "posts/show", type: :view do
  # before(:each) do
  #   @post = assign(:post, Post.create!(
  #     :name => "Name",
  #     :content => "MyString is a lovely string. It has more than 100 characters which means that it is valid and can be sent through my keyword extractor."
  #   ))
  # end

  # it "renders attributes in <p>" do
  #   render
  #   expect(rendered).to match(/Name/)
  #   expect(rendered).to match(/MyString is a lovely string. It has more than 100 characters which means that it is valid and can be sent through my keyword extractor./)
  # end
end
